import React, { PureComponent } from 'react';
import styles from './styles.css';

export default class MyComponent extends PureComponent {
  render() {
    return (
      <div styleName="example_style">
      </div>
    );
  }
}
